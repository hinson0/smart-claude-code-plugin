interface:
  display_name: "Markdown to HTML"
  short_description: "Convert Markdown into a safe, self-contained HTML file"
  default_prompt: "Use $fuzz:html to convert this Markdown document to HTML."
