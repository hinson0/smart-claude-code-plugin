interface:
  display_name: "Read-only guidance"
  short_description: "Provide concise guidance without running commands or changing files"
  default_prompt: "Use $fuzz:ask to provide concise read-only guidance."

policy:
  allow_implicit_invocation: false
